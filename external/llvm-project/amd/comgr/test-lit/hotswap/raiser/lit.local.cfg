# The refusal tests in this directory drive the loader with malformed code
# objects built by yaml2obj; the AMDGPU assembler rejects such inputs, so they
# cannot be .s files. Enable .yaml discovery for this directory only, leaving
# the suite's data-only .yaml files elsewhere untouched.
config.suffixes = config.suffixes | {".yaml"}
