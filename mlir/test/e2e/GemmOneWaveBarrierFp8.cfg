# Require MFMA or WMMA support and fp8 acceleration
if not config.arch_support_mfma and not config.arch_support_wmma:
  config.unsupported = True
if not config.arch_support_accel_fp8:
  config.unsupported = True
