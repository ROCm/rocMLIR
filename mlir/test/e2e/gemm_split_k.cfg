print(f"----> (in gemm_split_k/lit.local.cfg): config.features = \"{getattr(config, 'features', '<None>')}\" <----")

if not getattr(config, 'features', False) or not 'atomic_add' in config.features:
  config.unsupported = True
