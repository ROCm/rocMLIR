import importlib
import sys

# Test file current forces python3.7, because it needs >=3.7.
# Test requires tensorflow (which should be a recent tf-nightly).
if (sys.version_info.major < 3 or sys.version_info.minor < 7
    or not importlib.util.find_spec('tensorflow')):
  config.unsupported = True
