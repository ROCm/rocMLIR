# Conditionally skip tests based on architecture
if not config.arch_support_mfma and not config.arch_support_wmma:
    # this test uses accel perfConfig
    config.unsupported = ['multioperands_fold_backend_bug.mlir']
