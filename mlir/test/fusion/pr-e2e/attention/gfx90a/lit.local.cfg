# Tests in this directory require gfx90a (e.g. Specific perf_config that is only
# available on gfx90a)
if not (hasattr(config, 'arch') and config.arch and 'gfx90a' in config.arch):
  config.unsupported = True
