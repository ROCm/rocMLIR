# Tests in this directory require gfx950 (e.g. Specific perf_config that is only
# available on gfx950)
if not (hasattr(config, 'arch') and config.arch and 'gfx950' in config.arch):
  config.unsupported = True
