if not (hasattr(config, 'features') and config.features and 'atomic_add' in config.features) or not (hasattr(config, 'arch') and config.arch and "gfx950" in config.arch):
  config.unsupported = True

