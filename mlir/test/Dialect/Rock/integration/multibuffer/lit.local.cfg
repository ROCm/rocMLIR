print(f"   ----> config.features (in multibuffer) = \"{getattr(config, 'features', '<None>')}\" <----")
print(f"   ----> vars(config) (in multibuffer) = \"{vars(config)}\" <----")

if (not config.arch_support_mfma):
  config.unsupported = True
