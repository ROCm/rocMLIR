import os

# Check if hipblaslt-benchmark-driver exists
hipblaslt_driver = os.path.join(config.mlir_rock_tools_dir, 'hipblaslt-benchmark-driver')
if not os.path.exists(hipblaslt_driver):
    config.unsupported = True

# Only run for nightly tests (not PR tests)
if not config.enable_rock_driver_e2e_test or config.no_AMD_GPU:
    config.unsupported = True
