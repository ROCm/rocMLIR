# FP8 is only supported on gfx94x (MI300 series), gfx95x, and gfx12xx (Navi4)
if not (hasattr(config, 'arch') and config.arch and any(x in config.arch for x in ["gfx94", "gfx95", "gfx12"])):
    config.unsupported = True

