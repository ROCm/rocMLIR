find_package(composable_kernel 1.0.0 COMPONENTS device_operations CONFIG)
if (composable_kernel_FOUND)
  find_package(hip REQUIRED)
  add_executable(ck-benchmark-driver EXCLUDE_FROM_ALL ck-benchmark-driver.cpp)
  target_link_libraries(ck-benchmark-driver PRIVATE composable_kernel::device_operations hip::device benchmark-driver-utils)
  set_target_properties(ck-benchmark-driver PROPERTIES RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR}/bin)
else()
  message(STATUS "Not building a CK benchmark driver")
endif()
